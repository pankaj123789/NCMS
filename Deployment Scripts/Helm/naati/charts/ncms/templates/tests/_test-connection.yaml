apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ncms.fullname" . }}-test-connection"
  labels:
{{ include "ncms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ncms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
