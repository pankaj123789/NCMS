apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mynaati.fullname" . }}-test-connection"
  labels:
{{ include "mynaati.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mynaati.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
