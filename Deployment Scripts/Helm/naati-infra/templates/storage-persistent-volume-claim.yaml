apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "naati-infra.volumeClaimName" . }}	
spec:
  accessModes:
    - ReadWriteMany 
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeName: {{ include "naati-infra.persistentStorage" . }}	