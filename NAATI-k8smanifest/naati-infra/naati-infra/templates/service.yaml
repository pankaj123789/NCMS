apiVersion: v1
kind: Service
metadata:
  name: {{ include "naati-infra.fullname" . }}
  labels:
    {{- include "naati-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "naati-infra.selectorLabels" . | nindent 4 }}
